{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "WorkflowSource": {
    // Local dev fetches definitions from the public milestones-config repo. Override with LocalPath in
    // appsettings.local.json to use a local checkout instead.
    "RepoUrl": "https://github.com/UvA-FNWI/milestones-config"
  }
}
