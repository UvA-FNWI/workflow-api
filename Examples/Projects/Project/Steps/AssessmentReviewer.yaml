name: AssessmentReviewer
title: Assessment by reviewer
icon:
  type: checkmark-line

actions:
  - roles: [Reviewer]
    type: Submit
    label: Assess final version (reviewer)
    form: AssessmentReviewer

properties:
  - name: AssessmentReviewer
    type: Assessment!
    
progressText:
  en: Waits {{ coalesce(formatDate(UploadEvent, =dd/MM), =) }} for assessment
  nl: Wacht {{ coalesce(formatDate(UploadEvent, =dd-MM), =) }} op beoordeling

ends:
  event: AssessmentReviewer