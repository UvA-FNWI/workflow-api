name: AssessmentSupervisor
title: Assessment by supervisor
icon:
  type: checkmark-line

actions:
  - roles: [Supervisor]
    type: Submit
    label: Assess final version (supervisor)
    form: AssessmentSupervisor

properties:
  - name: AssessmentSupervisor
    type: Assessment!

progressText:
  en: "Waits ({{ formatDate(UploadEvent, =dd/MM) }}) for assessment"
  nl: "Wacht ({{ formatDate(UploadEvent, =dd-MM) }}) op beoordeling"
  
ends:
  event: AssessmentSupervisor