name: ProposalAssessment

title:
  en: Assessment
  nl: Beoordeling

progress:
  color: red
  text:
    en: Waiting for approval {{ coalesce(formatDate(StartEvent, =dd-MM-yyyy), =) }}
    nl: Wacht op goedkeuring {{ coalesce(formatDate(StartEvent, =dd-MM-yyyy), =) }}

hierarchyMode: Parallel

children:
  - ProposalAssessmentSupervisor
  - ProposalAssessmentReviewer
