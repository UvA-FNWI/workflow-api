name: AssessmentThesis
resultsType: AssessmentPartOverview
title:
  en: Assessment thesis
  nl: Beoordeling scriptie

progress:
  color: red
  text:
    en: Waiting for assessment thesis {{ coalesce(formatDate(UploadFinalThesisEvent, =dd-MM-yyyy), =) }}
    nl: Wacht op beoordeling scriptie {{ coalesce(formatDate(UploadFinalThesisEvent, =dd-MM-yyyy), =) }}

actions:
  - roles: [Supervisor]
    type: Edit
    form: AssessmentSupervisor
  - roles: [SecondReviewer]
    type: Edit
    form: AssessmentSecondReader

children:
  - AssessmentIndividual
  - AssessmentCombined
